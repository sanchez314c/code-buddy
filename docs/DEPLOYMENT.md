# Deployment

Code Buddy is a local CLI tool. There is no server deployment.

## Distribution

1. Push to GitHub
2. Users clone and run `./install.sh`
3. That's it

## Packaging

No package manager distribution yet. Future options:
- Homebrew tap
- AUR package
- npm wrapper (for npx usage)
